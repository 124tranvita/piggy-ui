const images = {
  logo: require('./logo.svg').default,
  control: require('./control.svg').default,
  dashboard: require('./dashboard.svg').default,
  profile: require('./profile.svg').default,
  income: require('./income.svg').default,
  catalogue: require('./catalogue.svg').default,
  spending: require('./spending.svg').default,
  report: require('./report.svg').default,
};

export default images;
